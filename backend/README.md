# TruEstate Sales Backend

Node.js + Express API that loads the CSV sales dataset into memory and exposes endpoints for search, filtering, sorting and pagination.

## Scripts

```bash
npm install
npm run dev
# or
npm start
